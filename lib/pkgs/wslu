# shellcheck shell=bash
####################################################################
# wslu
####################################################################
# Ragdata's Dotfiles - Application Manager
#
# File:         wslu
# Author:       Ragdata
# Date:         22/08/2024
# License:      MIT License
# Repository:	https://github.com/Ragdata/.dotfiles
# Copyright:    Copyright © 2024 Redeyed Technologies
####################################################################

#-------------------------------------------------------------------
# CHECK
#-------------------------------------------------------------------
wslu::check() { command -v wslu &> /dev/null; }

#-------------------------------------------------------------------
# INSTALL
#-------------------------------------------------------------------
wslu::install()
{
	local result

	echoInfo "Installing additional packages for 'apt': " -n
	sudo apt-get -qq -y install wslu; result=$?
	if [ "$result" -eq 0 ]; then echoSuccess "SUCCESS!"; else echoWarning "FAILED!"; fi

	echoInfo "Running garbage collection"
	sudo apt-get -qq -y clean && sudo apt-get -qq -y autoremove
	return $result
}

#-------------------------------------------------------------------
# REMOVE
#-------------------------------------------------------------------
wslu::remove()
{
	local result

	echoInfo "Removing 'apt': " -n
	sudo apt-get -qq -y purge wslu; result=$?
	if [ "$result" -eq 0 ]; then echoSuccess "SUCCESS!"; else echoWarning "FAILED!"; fi

	echoInfo "Running garbage collection"
	sudo apt-get -qq -y clean && sudo apt-get -qq -y autoremove
	return $result
}
